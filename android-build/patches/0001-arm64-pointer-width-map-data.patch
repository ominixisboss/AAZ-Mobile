diff --git a/android/app/src/main/cpp/CMakeLists.txt b/android/app/src/main/cpp/CMakeLists.txt
index 6c607dd85..3f3716a2c 100644
--- a/android/app/src/main/cpp/CMakeLists.txt
+++ b/android/app/src/main/cpp/CMakeLists.txt
@@ -53,7 +53,7 @@ foreach(SOURCE IN LISTS DATA_ASM SONG_ASM)
         add_custom_command(
             OUTPUT "${OUTPUT}"
             COMMAND "${CMAKE_COMMAND}" -E make_directory "${OUTPUT_DIR}"
-            COMMAND /bin/sh -c "sed -e 's/\\.4byte/\\.word/g;s/\\.2byte/\\.short/g' '${SOURCE}' > '${OUTPUT}.s' && '${ARM_NONE_EABI_AS}' -march=armv7-a -I'${ROOT}/sound' -o '${OUTPUT}' '${OUTPUT}.s'"
+            COMMAND /bin/sh -c "sed -e 's/\\.4byte/\\.word/g;s/\\.2byte/\\.short/g' '${SOURCE}' > '${OUTPUT}.s' && '${ARM_NONE_EABI_AS}' -march=armv7-a -I'${ROOT}' -I'${ROOT}/sound' -o '${OUTPUT}' '${OUTPUT}.s'"
             DEPENDS "${SOURCE}"
             VERBATIM)
     else()
diff --git a/asm/macros.inc b/asm/macros.inc
index 95bb4e31b..ef463499f 100644
--- a/asm/macros.inc
+++ b/asm/macros.inc
@@ -1,3 +1,4 @@
+	.include "asm/macros/ptr.inc"
 	.include "asm/macros/asm.inc"
 	.include "asm/macros/function.inc"
 	.include "asm/macros/movement.inc"
diff --git a/asm/macros/event.inc b/asm/macros/event.inc
index 3d6441687..bb4cf5c23 100644
--- a/asm/macros/event.inc
+++ b/asm/macros/event.inc
@@ -24,27 +24,27 @@
 	/* Jumps to destination and continues script execution from there. The location of the calling script is remembered and can be returned to later.*/
 	.macro call destination:req
 	.byte SCR_OP_CALL
-	.int \destination
+	ptr_stream \destination
 	.endm
 
 	/* Jumps to destination and continues script execution from there.*/
 	.macro goto destination:req
 	.byte SCR_OP_GOTO
-	.int \destination
+	ptr_stream \destination
 	.endm
 
 	/* If the result of the last comparison matches condition (see Comparison operators), jumps to destination and continues script execution from there.*/
 	.macro goto_if condition:req, destination:req
 	.byte SCR_OP_GOTO_IF
 	.byte \condition
-	.int \destination
+	ptr_stream \destination
 	.endm
 
 	/* If the result of the last comparison matches condition (see Comparison operators), calls destination.*/
 	.macro call_if condition:req, destination:req
 	.byte SCR_OP_CALL_IF
 	.byte \condition
-	.int \destination
+	ptr_stream \destination
 	.endm
 
 	/* Jumps to the script in gStdScripts at index function.*/
@@ -99,7 +99,7 @@
 	.macro loadword destIndex:req, value:req
 	.byte SCR_OP_LOAD_WORD
 	.byte \destIndex
-	.int \value
+	ptr_stream \value
 	.endm
 
 	/* Sets the value at the specified script data index to a fixed byte value.*/
@@ -113,21 +113,21 @@
 	.macro setptr value:req, ptr:req
 	.byte SCR_OP_SETPTR
 	.byte \value
-	.int \ptr
+	ptr_stream \ptr
 	.endm
 
 	/* Sets the value at the specified script data index to the value at pointer 'source'.*/
 	.macro loadbytefromptr destIndex:req, source:req
 	.byte SCR_OP_LOADBYTEFROMPTR
 	.byte \destIndex
-	.int \source
+	ptr_stream \source
 	.endm
 
 	/* Sets the value at pointer 'destination' to the contents of the script data at 'srcIndex'.*/
 	.macro setptrbyte srcIndex:req, destination:req
 	.byte SCR_OP_SETPTRBYTE
 	.byte \srcIndex
-	.int \destination
+	ptr_stream \destination
 	.endm
 
 	/* Copies the contents of the script data from one index to another.*/
@@ -140,8 +140,8 @@
 	/* Copies the byte at source to destination, replacing whatever byte was previously there.*/
 	.macro copybyte destination:req, source:req
 	.byte SCR_OP_COPYBYTE
-	.int \destination
-	.int \source
+	ptr_stream \destination
+	ptr_stream \source
 	.endm
 
 	@ Changes the value of destination to value.
@@ -203,14 +203,14 @@
 	.macro compare_local_to_ptr local:req, ptr:req
 	.byte SCR_OP_COMPARE_LOCAL_TO_PTR
 	.byte \local
-	.int \ptr
+	ptr_stream \ptr
 	.endm
 
 	/* Compares the value at 'ptr' to the value of the script data at index 'local'.*/
 	/* The result is stored in comparisonResult to be acted on by goto_if / call_if*/
 	.macro compare_ptr_to_local ptr:req, local:req
 	.byte SCR_OP_COMPARE_PTR_TO_LOCAL
-	.int \ptr
+	ptr_stream \ptr
 	.byte \local
 	.endm
 
@@ -218,7 +218,7 @@
 	/* The result is stored in comparisonResult to be acted on by goto_if / call_if*/
 	.macro compare_ptr_to_value ptr:req, value:req
 	.byte SCR_OP_COMPARE_PTR_TO_VALUE
-	.int \ptr
+	ptr_stream \ptr
 	.byte \value
 	.endm
 
@@ -226,8 +226,8 @@
 	/* The result is stored in comparisonResult to be acted on by goto_if / call_if*/
 	.macro compare_ptr_to_ptr ptr1:req, ptr2:req
 	.byte SCR_OP_COMPARE_PTR_TO_PTR
-	.int \ptr1
-	.int \ptr2
+	ptr_stream \ptr1
+	ptr_stream \ptr2
 	.endm
 
 	/* Compares the value of 'var' to a fixed value.*/
@@ -259,13 +259,13 @@
 	/* Calls the native C function stored at func.*/
 	.macro callnative func:req
 	.byte SCR_OP_CALLNATIVE
-	.int \func
+	ptr_stream \func
 	.endm
 
 	/* Replaces the script with the function stored at func. Execution returns to the bytecode script when func returns TRUE.*/
 	.macro gotonative func:req
 	.byte SCR_OP_GOTONATIVE
-	.int \func
+	ptr_stream \func
 	.endm
 
 	/* Calls a function listed in the table in data/specials.inc.*/
@@ -599,12 +599,12 @@
 		.ifb \map
 			.byte SCR_OP_APPLYMOVEMENT
 			.short \localId
-			.int \movements
+			ptr_stream \movements
 		.else
 			@ Really only useful if the object has followed from one map to another (e.g. Wally during the catching event).
 			.byte SCR_OP_APPLYMOVEMENTAT
 			.short \localId
-			.int \movements
+			ptr_stream \movements
 			map \map
 		.endif
 	.endm
@@ -693,51 +693,51 @@
 	.short \trainer
 	.short \local_id
 	.if \type == TRAINER_BATTLE_SINGLE
-		.int \pointer1 /* text*/
-		.int \pointer2 /* text*/
+		ptr_stream \pointer1 /* text*/
+		ptr_stream \pointer2 /* text*/
 	.elseif \type == TRAINER_BATTLE_CONTINUE_SCRIPT_NO_MUSIC
-		.int \pointer1 /* text*/
-		.int \pointer2 /* text*/
-		.int \pointer3 /* event script*/
+		ptr_stream \pointer1 /* text*/
+		ptr_stream \pointer2 /* text*/
+		ptr_stream \pointer3 /* event script*/
 	.elseif \type == TRAINER_BATTLE_CONTINUE_SCRIPT
-		.int \pointer1 /* text*/
-		.int \pointer2 /* text*/
-		.int \pointer3 /* event script*/
+		ptr_stream \pointer1 /* text*/
+		ptr_stream \pointer2 /* text*/
+		ptr_stream \pointer3 /* event script*/
 	.elseif \type == TRAINER_BATTLE_SINGLE_NO_INTRO_TEXT
-		.int \pointer1 /* text*/
+		ptr_stream \pointer1 /* text*/
 	.elseif \type == TRAINER_BATTLE_DOUBLE
-		.int \pointer1 /* text*/
-		.int \pointer2 /* text*/
-		.int \pointer3 /* text*/
+		ptr_stream \pointer1 /* text*/
+		ptr_stream \pointer2 /* text*/
+		ptr_stream \pointer3 /* text*/
 	.elseif \type == TRAINER_BATTLE_REMATCH
-		.int \pointer1 /* text*/
-		.int \pointer2 /* text*/
+		ptr_stream \pointer1 /* text*/
+		ptr_stream \pointer2 /* text*/
 	.elseif \type == TRAINER_BATTLE_CONTINUE_SCRIPT_DOUBLE
-		.int \pointer1 /* text*/
-		.int \pointer2 /* text*/
-		.int \pointer3 /* text*/
-		.int \pointer4 /* event script*/
+		ptr_stream \pointer1 /* text*/
+		ptr_stream \pointer2 /* text*/
+		ptr_stream \pointer3 /* text*/
+		ptr_stream \pointer4 /* event script*/
 	.elseif \type == TRAINER_BATTLE_REMATCH_DOUBLE
-		.int \pointer1 /* text*/
-		.int \pointer2 /* text*/
-		.int \pointer3 /* text*/
+		ptr_stream \pointer1 /* text*/
+		ptr_stream \pointer2 /* text*/
+		ptr_stream \pointer3 /* text*/
 	.elseif \type == TRAINER_BATTLE_CONTINUE_SCRIPT_DOUBLE_NO_MUSIC
-		.int \pointer1 /* text*/
-		.int \pointer2 /* text*/
-		.int \pointer3 /* text*/
-		.int \pointer4 /* event script*/
+		ptr_stream \pointer1 /* text*/
+		ptr_stream \pointer2 /* text*/
+		ptr_stream \pointer3 /* text*/
+		ptr_stream \pointer4 /* event script*/
 	.elseif \type == TRAINER_BATTLE_PYRAMID
-		.int \pointer1 /* text*/
-		.int \pointer2 /* text*/
+		ptr_stream \pointer1 /* text*/
+		ptr_stream \pointer2 /* text*/
 	.elseif \type == TRAINER_BATTLE_SET_TRAINER_A
-		.int \pointer1 /* text*/
-		.int \pointer2 /* text*/
+		ptr_stream \pointer1 /* text*/
+		ptr_stream \pointer2 /* text*/
 	.elseif \type == TRAINER_BATTLE_SET_TRAINER_B
-		.int \pointer1 /* text*/
-		.int \pointer2 /* text*/
+		ptr_stream \pointer1 /* text*/
+		ptr_stream \pointer2 /* text*/
 	.elseif \type == TRAINER_BATTLE_HILL
-		.int \pointer1 /* text*/
-		.int \pointer2 /* text*/
+		ptr_stream \pointer1 /* text*/
+		ptr_stream \pointer2 /* text*/
 	.endif
 	.endm
 
@@ -851,7 +851,7 @@
 	/* a pointer to the text. The 'loadword 0' in msgbox sets this value, for instance.*/
 	.macro message text:req
 	.byte SCR_OP_MESSAGE
-	.int \text
+	ptr_stream \text
 	.endm
 
 	/* Closes the current message box.*/
@@ -976,7 +976,7 @@
 	/* bytes determined the box's size and position, but in Emerald these are calculated automatically).*/
 	.macro braillemessage text:req
 	.byte SCR_OP_BRAILLEMESSAGE
-	.int \text
+	ptr_stream \text
 	.endm
 
 	/* Formatting for the braille window, to be put at the start of a pointer used by braillemessage.*/
@@ -1104,14 +1104,14 @@
 	.macro bufferstring stringVarId:req, text:req
 	.byte SCR_OP_BUFFERSTRING
 	stringvar \stringVarId
-	.int \text
+	ptr_stream \text
 	.endm
 
 	/* Opens the Pokemart system, offering the specified products for sale.*/
 	/* Products should be a list of .short item values preceded by an .align 2*/
 	.macro pokemart products:req
 	.byte SCR_OP_POKEMART
-	.int \products
+	ptr_stream \products
 	.endm
 
 	@ Used as the endpoint for a Pokemart item list
@@ -1125,13 +1125,13 @@
 	@ Products should be a list of .short decoration values preceded by an .align 2
 	.macro pokemartdecoration products:req
 	.byte SCR_OP_POKEMARTDECORATION
-	.int \products
+	ptr_stream \products
 	.endm
 
 	/* Identical to pokemartdecoration, but with slight changes to the clerk dialogue. See uses of MART_TYPE_DECOR2.*/
 	.macro pokemartdecoration2 products:req
 	.byte SCR_OP_POKEMARTDECORATION2
-	.int \products
+	ptr_stream \products
 	.endm
 
 	/* Starts up the slot machine minigame. id is a SLOT_MACHINE_* value that influences probabilities of certain reel outcomes.*/
@@ -1260,7 +1260,7 @@
 	/* Automatically scrolls through the message without player input and at a fixed speed.*/
 	.macro messageautoscroll text:req
 	.byte SCR_OP_MESSAGEAUTOSCROLL
-	.int \text
+	ptr_stream \text
 	.endm
 
 	/* Executes the specified field effect animation (FLDEFF_*).*/
@@ -1460,52 +1460,52 @@
 	/* Sets a relative address to be used by the other vcommands as part of a Mystery Gift script.*/
 	.macro setvaddress pointer:req
 	.byte SCR_OP_SETVADDRESS
-	.int \pointer
+	ptr_stream \pointer
 	.endm
 
 	/* Equivalent to goto using the relative address set by setvaddress.*/
 	.macro vgoto destination:req
 	.byte SCR_OP_VGOTO
-	.int \destination
+	ptr_stream \destination
 	.endm
 
 	/* Equivalent to call using the relative address set by setvaddress.*/
 	.macro vcall destination:req
 	.byte SCR_OP_VCALL
-	.int \destination
+	ptr_stream \destination
 	.endm
 
 	/* Equivalent to goto_if using the relative address set by setvaddress.*/
 	.macro vgoto_if condition:req, destination:req
 	.byte SCR_OP_VGOTO_IF
 	.byte \condition
-	.int \destination
+	ptr_stream \destination
 	.endm
 
 	/* Equivalent to call_if using the relative address set by setvaddress.*/
 	.macro vcall_if condition:req, destination:req
 	.byte SCR_OP_VCALL_IF
 	.byte \condition
-	.int \destination
+	ptr_stream \destination
 	.endm
 
 	/* Equivalent to message using the relative address set by setvaddress.*/
 	.macro vmessage text:req
 	.byte SCR_OP_VMESSAGE
-	.int \text
+	ptr_stream \text
 	.endm
 
 	/* Expands the given text at the pointer (- the relative address set by setvaddress) into gStringVar4*/
 	.macro vbuffermessage text:req
 	.byte SCR_OP_VBUFFERMESSAGE
-	.int \text
+	ptr_stream \text
 	.endm
 
 	/* Equivalent to bufferstring using the relative address set by setvaddress.*/
 	.macro vbufferstring stringVarIndex:req, text:req
 	.byte SCR_OP_VBUFFERSTRING
 	stringvar \stringVarIndex
-	.int \text
+	ptr_stream \text
 	.endm
 
 	/* Create a window showing how many Coins the player has.*/
@@ -1564,7 +1564,7 @@
 	/* Used only in FireRed/LeafGreen, does nothing in Emerald.*/
 	.macro loadhelp text:req
 	.byte SCR_OP_LOADHELP
-	.int \text
+	ptr_stream \text
 	.endm
 
 	/* Used only in FireRed/LeafGreen, does nothing in Emerald.*/
@@ -1684,7 +1684,7 @@
 	/* Does not wait for player input to continue.*/
 	.macro messageinstant text:req
 	.byte SCR_OP_MESSAGEINSTANT
-	.int \text
+	ptr_stream \text
 	.endm
 
 	/* Equivalent to fadescreen but copies gPlttBufferUnfaded to gPaletteDecompressionBuffer on the fade out*/
@@ -1713,7 +1713,7 @@
 	/* Starts a Pokenav call with the given text.*/
 	.macro pokenavcall text:req
 	.byte SCR_OP_POKENAVCALL
-	.int \text
+	ptr_stream \text
 	.endm
 
 	/* Warp with a fade to white. Used during the Sootopolis legendary fight.*/
diff --git a/asm/macros/m4a.inc b/asm/macros/m4a.inc
index d6e279a6d..182355579 100644
--- a/asm/macros/m4a.inc
+++ b/asm/macros/m4a.inc
@@ -1,25 +1,33 @@
+	.include "asm/macros/ptr.inc"
 	.macro song label:req, music_player:req, unknown:req
-	.int \label
+	ptr \label
 	.short \music_player
 	.short \unknown
 	.endm
 
 	.macro music_player info_struct:req, track_struct:req, num_tracks:req, unknown:req
-	.int \info_struct
-	.int \track_struct
+	ptr \info_struct
+	ptr \track_struct
 	.byte \num_tracks
 	.space 1  /* Padding*/
 	.short \unknown
 	.endm
 
 	.macro voice_group label:req, starting_note
-	.align 2
+	palign
 	.ifb \starting_note
 		.global voicegroup_\label 
 		voicegroup_\label:
+	.else
+	// Indexes backwards by sizeof(struct ToneData), which is 12 bytes on a
+	// 32-bit target and 24 on a 64-bit one: 4 u8, 4 pad, an 8-byte pointer,
+	// 4 u8, 4 pad. Verified against the compiler, not hand-computed.
+	.ifdef PTR64
+		.set voicegroup_\label, . - \starting_note * 0x18
 	.else
 		.set voicegroup_\label, . - \starting_note * 0xC
 	.endif
+	.endif
 	.endm
 
 	.macro keysplit label:req, starting_note
diff --git a/asm/macros/map.inc b/asm/macros/map.inc
index 6e91c390c..02ff367d3 100644
--- a/asm/macros/map.inc
+++ b/asm/macros/map.inc
@@ -9,14 +9,14 @@
 	/* Defines a map script. 'type' is any MAP_SCRIPT_* constant (see include/constants/map_scripts.h)*/
 	.macro map_script type:req, script:req
 	.byte \type
-	.int \script
+	ptr \script
 	.endm
 
 	/* Defines an entry in a map script table (for either ON_WARP_INTO_MAP_TABLE or ON_FRAME_TABLE)*/
 	.macro map_script_2 var:req, compare:req, script:req
 	.short \var
 	.short \compare
-	.int \script
+	ptr \script
 	.endm
 
 	/* Defines an object event template for map data, to be used by a normal object. Mirrors the struct layout of ObjectEventTemplate in include/global.fieldmap.h*/
@@ -32,7 +32,7 @@
 	.space 1  /* Padding*/
 	.short \trainer_type
 	.short \sight_radius_tree_etc
-	.int \script
+	ptr \script
 	.short \event_flag
 	.space 2  /* Padding*/
 	inc _num_npcs
@@ -72,7 +72,7 @@
 	.2byte \var
 	.2byte \varValue
 	.space 2  /* Padding*/
-	.4byte \script
+	ptr \script
 	inc _num_traps
 	.endm
 
@@ -90,10 +90,13 @@
 	.byte \kind
 	.space 2  /* Padding*/
 	.if \kind != BG_EVENT_HIDDEN_ITEM
-		.int \arg6
+		ptr \arg6
 	.else
 		.short \arg6
 		.short \arg7
+		.ifdef PTR64
+		.space 4
+		.endif
 	.endif
 	inc _num_signs
 	.endm
@@ -116,7 +119,10 @@
 	/* Defines the table of event data for a map. Mirrors the struct layout of MapEvents in include/global.fieldmap.h*/
 	.macro map_events npcs:req, warps:req, traps:req, signs:req
 	.byte _num_npcs, _num_warps, _num_traps, _num_signs
-	.int \npcs, \warps, \traps, \signs
+	ptr \npcs
+	ptr \warps
+	ptr \traps
+	ptr \signs
 	reset_map_events
 	.endm
 
diff --git a/asm/macros/music_voice.inc b/asm/macros/music_voice.inc
index 04a734324..7f4fbb77f 100644
--- a/asm/macros/music_voice.inc
+++ b/asm/macros/music_voice.inc
@@ -1,3 +1,4 @@
+	.include "asm/macros/ptr.inc"
 	.macro voice_directsound base_midi_key:req, pan:req, sample_data_pointer:req, attack:req, decay:req, sustain:req, release:req
 	.byte 0
 	_voice_directsound \base_midi_key, \pan, \sample_data_pointer, \attack, \decay, \sustain, \release
@@ -21,7 +22,7 @@
 	.else
 	.byte 0
 	.endif
-	.int \sample_data_pointer
+	ptr \sample_data_pointer
 	.byte \attack
 	.byte \decay
 	.byte \sustain
@@ -95,7 +96,7 @@
 	.byte 0
 	.endif
 	.byte 0
-	.int \wave_samples_pointer
+	ptr \wave_samples_pointer
 	.byte (\attack  & 0x7)
 	.byte (\decay   & 0x7)
 	.byte (\sustain & 0xF)
@@ -129,24 +130,24 @@
 
 	.macro voice_keysplit voice_group_pointer:req, keysplit_table_pointer:req
 	.byte 0x40, 0, 0, 0
-	.int \voice_group_pointer
-	.int \keysplit_table_pointer
+	ptr \voice_group_pointer
+	ptr \keysplit_table_pointer
 	.endm
 
 	.macro voice_keysplit_all voice_group_pointer:req
 	.byte 0x80, 0, 0, 0
-	.int \voice_group_pointer
-	.int 0
+	ptr \voice_group_pointer
+	ptr 0
 	.endm
 
 	.macro cry sample:req
 	.byte 0x20, 60, 0, 0
-	.int \sample
+	ptr \sample
 	.byte 0xff, 0, 0xff, 0
 	.endm
 
 	.macro cry_reverse sample:req
 	.byte 0x30, 60, 0, 0
-	.int \sample
+	ptr \sample
 	.byte 0xff, 0, 0xff, 0
 	.endm
diff --git a/asm/macros/ptr.inc b/asm/macros/ptr.inc
new file mode 100644
index 000000000..7bdd6c328
--- /dev/null
+++ b/asm/macros/ptr.inc
@@ -0,0 +1,47 @@
+// Pointer emission that adapts to the target's pointer width.
+//
+// The decomp emits data pointers as .4byte/.int, which is correct for a 32-bit
+// target but produces R_AARCH64_ABS32 relocations on arm64 -- and an absolute
+// 32-bit relocation against a symbol cannot exist in a shared object, which is
+// what an Android app always is.
+//
+// Assemble with --defsym PTR64=1 for a 64-bit target; leaving it undefined
+// keeps the original 32-bit output byte for byte, so the existing armeabi-v7a
+// build needs no changes. The .balign inside `ptr` mirrors exactly what the C
+// compiler does when it aligns a pointer field, so struct layouts agree.
+
+// Include guard: this file is pulled in from asm/macros.inc and directly
+// from the sound macros, and gas rejects a duplicate .macro definition.
+	.ifdef PTR_INC_INCLUDED
+	.else
+	.equiv PTR_INC_INCLUDED, 1
+
+	.macro palign
+	.ifdef PTR64
+	.balign 8
+	.else
+	.balign 4
+	.endif
+	.endm
+
+	.macro ptr label:req
+	.ifdef PTR64
+	.balign 8
+	.xword \label
+	.else
+	.4byte \label
+	.endif
+	.endm
+	// Pointer inside a bytecode stream. Identical to `ptr` except that it
+	// never aligns: script bytecode is walked sequentially by an interpreter
+	// that has no idea padding exists, so inserting any would desynchronise
+	// it. Unaligned .xword is legal and relocates fine on arm64.
+	.macro ptr_stream label:req
+	.ifdef PTR64
+	.xword \label
+	.else
+	.4byte \label
+	.endif
+	.endm
+
+	.endif
diff --git a/data/event_scripts.s b/data/event_scripts.s
index ba6f55c6d..f9f87150a 100644
--- a/data/event_scripts.s
+++ b/data/event_scripts.s
@@ -63,44 +63,46 @@
 	.set ALLOCATE_SCRIPT_CMD_TABLE, 1
 	.include "data/script_cmd_table.inc"
 
+	palign
 gSpecialVars::
-	.int gSpecialVar_0x8000
-	.int gSpecialVar_0x8001
-	.int gSpecialVar_0x8002
-	.int gSpecialVar_0x8003
-	.int gSpecialVar_0x8004
-	.int gSpecialVar_0x8005
-	.int gSpecialVar_0x8006
-	.int gSpecialVar_0x8007
-	.int gSpecialVar_0x8008
-	.int gSpecialVar_0x8009
-	.int gSpecialVar_0x800A
-	.int gSpecialVar_0x800B
-	.int gSpecialVar_Facing
-	.int gSpecialVar_Result
-	.int gSpecialVar_ItemId
-	.int gSpecialVar_LastTalked
-	.int gSpecialVar_ContestRank
-	.int gSpecialVar_ContestCategory
-	.int gSpecialVar_MonBoxId
-	.int gSpecialVar_MonBoxPos
-	.int gSpecialVar_Unused_0x8014
-	.int gTrainerBattleOpponent_A
+	ptr gSpecialVar_0x8000
+	ptr gSpecialVar_0x8001
+	ptr gSpecialVar_0x8002
+	ptr gSpecialVar_0x8003
+	ptr gSpecialVar_0x8004
+	ptr gSpecialVar_0x8005
+	ptr gSpecialVar_0x8006
+	ptr gSpecialVar_0x8007
+	ptr gSpecialVar_0x8008
+	ptr gSpecialVar_0x8009
+	ptr gSpecialVar_0x800A
+	ptr gSpecialVar_0x800B
+	ptr gSpecialVar_Facing
+	ptr gSpecialVar_Result
+	ptr gSpecialVar_ItemId
+	ptr gSpecialVar_LastTalked
+	ptr gSpecialVar_ContestRank
+	ptr gSpecialVar_ContestCategory
+	ptr gSpecialVar_MonBoxId
+	ptr gSpecialVar_MonBoxPos
+	ptr gSpecialVar_Unused_0x8014
+	ptr gTrainerBattleOpponent_A
 
 	.include "data/specials.inc"
 
+	palign
 gStdScripts::
-	.int Std_ObtainItem              /* STD_OBTAIN_ITEM*/
-	.int Std_FindItem                /* STD_FIND_ITEM*/
-	.int Std_MsgboxNPC               /* MSGBOX_NPC*/
-	.int Std_MsgboxSign              /* MSGBOX_SIGN*/
-	.int Std_MsgboxDefault           /* MSGBOX_DEFAULT*/
-	.int Std_MsgboxYesNo             /* MSGBOX_YESNO*/
-	.int Std_MsgboxAutoclose         /* MSGBOX_AUTOCLOSE*/
-	.int Std_ObtainDecoration        /* STD_OBTAIN_DECORATION*/
-	.int Std_RegisteredInMatchCall   /* STD_REGISTER_MATCH_CALL*/
-	.int Std_MsgboxGetPoints         /* MSGBOX_GETPOINTS*/
-	.int Std_MsgboxPokenav           /* MSGBOX_POKENAV*/
+	ptr Std_ObtainItem              /* STD_OBTAIN_ITEM*/
+	ptr Std_FindItem                /* STD_FIND_ITEM*/
+	ptr Std_MsgboxNPC               /* MSGBOX_NPC*/
+	ptr Std_MsgboxSign              /* MSGBOX_SIGN*/
+	ptr Std_MsgboxDefault           /* MSGBOX_DEFAULT*/
+	ptr Std_MsgboxYesNo             /* MSGBOX_YESNO*/
+	ptr Std_MsgboxAutoclose         /* MSGBOX_AUTOCLOSE*/
+	ptr Std_ObtainDecoration        /* STD_OBTAIN_DECORATION*/
+	ptr Std_RegisteredInMatchCall   /* STD_REGISTER_MATCH_CALL*/
+	ptr Std_MsgboxGetPoints         /* MSGBOX_GETPOINTS*/
+	ptr Std_MsgboxPokenav           /* MSGBOX_POKENAV*/
 gStdScripts_End::
 
 	.include "data/maps/PetalburgCity/scripts.inc"
diff --git a/data/script_cmd_table.inc b/data/script_cmd_table.inc
index 206f17876..899ec7c90 100644
--- a/data/script_cmd_table.inc
+++ b/data/script_cmd_table.inc
@@ -2,7 +2,7 @@
 	.set SCRIPT_CMD_TABLE_ENTRY_MACRO, 1
 	.macro script_cmd_table_entry constant:req value:req
 	.if ALLOCATE_SCRIPT_CMD_TABLE
-	.4byte \value
+	ptr \value
 	.else
 	enum \constant
 	.endif
@@ -12,7 +12,7 @@
 
 	enum_start
 	.if ALLOCATE_SCRIPT_CMD_TABLE
-	.align 2
+	palign
 gScriptCmdTable::
 	.endif
 	script_cmd_table_entry SCR_OP_NOP                           ScrCmd_nop                            @ 0x00
@@ -245,5 +245,5 @@ gScriptCmdTable::
 
 	.if ALLOCATE_SCRIPT_CMD_TABLE
 gScriptCmdTableEnd::
-	.int ScrCmd_nop
+	ptr ScrCmd_nop
 	.endif
diff --git a/data/specials.inc b/data/specials.inc
index b8e3afd3d..0c7398038 100644
--- a/data/specials.inc
+++ b/data/specials.inc
@@ -2,7 +2,7 @@
 .global SPECIAL_\ptr
 .set SPECIAL_\ptr, __special__
 .set __special__, __special__ + 1
-	.int \ptr
+	ptr \ptr
 	.endm
 
 .set __special__, 0
diff --git a/include/script.h b/include/script.h
index 7c180e961..b2d1d9dab 100755
--- a/include/script.h
+++ b/include/script.h
@@ -16,7 +16,7 @@ struct ScriptContext
     const u8 *stack[20];
     ScrCmdFunc *cmdTable;
     ScrCmdFunc *cmdTableEnd;
-    u32 data[4];
+    uintptr_t data[4];
 };
 
 #define ScriptReadByte(ctx) (*(ctx->scriptPtr++))
@@ -30,6 +30,7 @@ void ScriptJump(struct ScriptContext *ctx, const u8 *ptr);
 void ScriptCall(struct ScriptContext *ctx, const u8 *ptr);
 void ScriptReturn(struct ScriptContext *ctx);
 u16 ScriptReadHalfword(struct ScriptContext *ctx);
+uintptr_t ScriptReadPtr(struct ScriptContext *ctx);
 u32 ScriptReadWord(struct ScriptContext *ctx);
 void LockPlayerFieldControls(void);
 void UnlockPlayerFieldControls(void);
diff --git a/src/scrcmd.c b/src/scrcmd.c
index 39a6ec7cc..5d3487054 100644
--- a/src/scrcmd.c
+++ b/src/scrcmd.c
@@ -54,7 +54,7 @@ typedef u16 (*SpecialFunc)(void);
 typedef void (*NativeFunc)(void);
 
 EWRAM_DATA const u8 *gRamScriptRetAddr = NULL;
-static EWRAM_DATA u32 sAddressOffset = 0; // For relative addressing in vgoto etc., used by saved scripts (e.g. Mystery Event)
+static EWRAM_DATA uintptr_t sAddressOffset = 0; // For relative addressing in vgoto etc., used by saved scripts (e.g. Mystery Event)
 static EWRAM_DATA u16 sPauseCounter = 0;
 static EWRAM_DATA u16 sMovingNpcId = 0;
 static EWRAM_DATA u16 sMovingNpcMapGroup = 0;
@@ -109,7 +109,7 @@ bool8 ScrCmd_end(struct ScriptContext *ctx)
 
 bool8 ScrCmd_gotonative(struct ScriptContext *ctx)
 {
-    bool8 (*addr)(void) = (bool8 (*)(void))ScriptReadWord(ctx);
+    bool8 (*addr)(void) = (bool8 (*)(void))ScriptReadPtr(ctx);
 
     SetupNativeScript(ctx, addr);
     return TRUE;
@@ -133,7 +133,7 @@ bool8 ScrCmd_specialvar(struct ScriptContext *ctx)
 
 bool8 ScrCmd_callnative(struct ScriptContext *ctx)
 {
-    NativeFunc func = (NativeFunc)ScriptReadWord(ctx);
+    NativeFunc func = (NativeFunc)ScriptReadPtr(ctx);
 
     func();
     return FALSE;
@@ -147,7 +147,7 @@ bool8 ScrCmd_waitstate(struct ScriptContext *ctx)
 
 bool8 ScrCmd_goto(struct ScriptContext *ctx)
 {
-    const u8 *ptr = (const u8 *)ScriptReadWord(ctx);
+    const u8 *ptr = (const u8 *)ScriptReadPtr(ctx);
 
     ScriptJump(ctx, ptr);
     return FALSE;
@@ -161,7 +161,7 @@ bool8 ScrCmd_return(struct ScriptContext *ctx)
 
 bool8 ScrCmd_call(struct ScriptContext *ctx)
 {
-    const u8 *ptr = (const u8 *)ScriptReadWord(ctx);
+    const u8 *ptr = (const u8 *)ScriptReadPtr(ctx);
 
     ScriptCall(ctx, ptr);
     return FALSE;
@@ -170,7 +170,7 @@ bool8 ScrCmd_call(struct ScriptContext *ctx)
 bool8 ScrCmd_goto_if(struct ScriptContext *ctx)
 {
     u8 condition = ScriptReadByte(ctx);
-    const u8 *ptr = (const u8 *)ScriptReadWord(ctx);
+    const u8 *ptr = (const u8 *)ScriptReadPtr(ctx);
 
     if (sScriptConditionTable[condition][ctx->comparisonResult] == 1)
         ScriptJump(ctx, ptr);
@@ -180,7 +180,7 @@ bool8 ScrCmd_goto_if(struct ScriptContext *ctx)
 bool8 ScrCmd_call_if(struct ScriptContext *ctx)
 {
     u8 condition = ScriptReadByte(ctx);
-    const u8 *ptr = (const u8 *)ScriptReadWord(ctx);
+    const u8 *ptr = (const u8 *)ScriptReadPtr(ctx);
 
     if (sScriptConditionTable[condition][ctx->comparisonResult] == 1)
         ScriptCall(ctx, ptr);
@@ -189,8 +189,8 @@ bool8 ScrCmd_call_if(struct ScriptContext *ctx)
 
 bool8 ScrCmd_setvaddress(struct ScriptContext *ctx)
 {
-    u32 addr1 = (u32)ctx->scriptPtr - 1;
-    u32 addr2 = ScriptReadWord(ctx);
+    uintptr_t addr1 = (uintptr_t)ctx->scriptPtr - 1;
+    uintptr_t addr2 = ScriptReadPtr(ctx);
 
     sAddressOffset = addr2 - addr1;
     return FALSE;
@@ -198,7 +198,7 @@ bool8 ScrCmd_setvaddress(struct ScriptContext *ctx)
 
 bool8 ScrCmd_vgoto(struct ScriptContext *ctx)
 {
-    u32 addr = ScriptReadWord(ctx);
+    uintptr_t addr = ScriptReadPtr(ctx);
 
     ScriptJump(ctx, (u8 *)(addr - sAddressOffset));
     return FALSE;
@@ -206,7 +206,7 @@ bool8 ScrCmd_vgoto(struct ScriptContext *ctx)
 
 bool8 ScrCmd_vcall(struct ScriptContext *ctx)
 {
-    u32 addr = ScriptReadWord(ctx);
+    uintptr_t addr = ScriptReadPtr(ctx);
 
     ScriptCall(ctx, (u8 *)(addr - sAddressOffset));
     return FALSE;
@@ -215,7 +215,7 @@ bool8 ScrCmd_vcall(struct ScriptContext *ctx)
 bool8 ScrCmd_vgoto_if(struct ScriptContext *ctx)
 {
     u8 condition = ScriptReadByte(ctx);
-    const u8 *ptr = (const u8 *)(ScriptReadWord(ctx) - sAddressOffset);
+    const u8 *ptr = (const u8 *)(ScriptReadPtr(ctx) - sAddressOffset);
 
     if (sScriptConditionTable[condition][ctx->comparisonResult] == 1)
         ScriptJump(ctx, ptr);
@@ -225,7 +225,7 @@ bool8 ScrCmd_vgoto_if(struct ScriptContext *ctx)
 bool8 ScrCmd_vcall_if(struct ScriptContext *ctx)
 {
     u8 condition = ScriptReadByte(ctx);
-    const u8 *ptr = (const u8 *)(ScriptReadWord(ctx) - sAddressOffset);
+    const u8 *ptr = (const u8 *)(ScriptReadPtr(ctx) - sAddressOffset);
 
     if (sScriptConditionTable[condition][ctx->comparisonResult] == 1)
         ScriptCall(ctx, ptr);
@@ -305,7 +305,7 @@ bool8 ScrCmd_loadword(struct ScriptContext *ctx)
 {
     u8 index = ScriptReadByte(ctx);
 
-    ctx->data[index] = ScriptReadWord(ctx);
+    ctx->data[index] = ScriptReadPtr(ctx);
     return FALSE;
 }
 
@@ -313,7 +313,7 @@ bool8 ScrCmd_loadbytefromptr(struct ScriptContext *ctx)
 {
     u8 index = ScriptReadByte(ctx);
 
-    ctx->data[index] = *(const u8 *)ScriptReadWord(ctx);
+    ctx->data[index] = *(const u8 *)ScriptReadPtr(ctx);
     return FALSE;
 }
 
@@ -321,7 +321,7 @@ bool8 ScrCmd_setptr(struct ScriptContext *ctx)
 {
     u8 value = ScriptReadByte(ctx);
 
-    *(u8 *)ScriptReadWord(ctx) = value;
+    *(u8 *)ScriptReadPtr(ctx) = value;
     return FALSE;
 }
 
@@ -337,7 +337,7 @@ bool8 ScrCmd_setptrbyte(struct ScriptContext *ctx)
 {
     u8 index = ScriptReadByte(ctx);
 
-    *(u8 *)ScriptReadWord(ctx) = ctx->data[index];
+    *(u8 *)ScriptReadPtr(ctx) = ctx->data[index];
     return FALSE;
 }
 
@@ -352,8 +352,8 @@ bool8 ScrCmd_copylocal(struct ScriptContext *ctx)
 
 bool8 ScrCmd_copybyte(struct ScriptContext *ctx)
 {
-    u8 *ptr = (u8 *)ScriptReadWord(ctx);
-    *ptr = *(const u8 *)ScriptReadWord(ctx);
+    u8 *ptr = (u8 *)ScriptReadPtr(ctx);
+    *ptr = *(const u8 *)ScriptReadPtr(ctx);
     return FALSE;
 }
 
@@ -412,7 +412,7 @@ bool8 ScrCmd_compare_local_to_value(struct ScriptContext *ctx)
 bool8 ScrCmd_compare_local_to_ptr(struct ScriptContext *ctx)
 {
     const u8 value1 = ctx->data[ScriptReadByte(ctx)];
-    const u8 value2 = *(const u8 *)ScriptReadWord(ctx);
+    const u8 value2 = *(const u8 *)ScriptReadPtr(ctx);
 
     ctx->comparisonResult = Compare(value1, value2);
     return FALSE;
@@ -420,7 +420,7 @@ bool8 ScrCmd_compare_local_to_ptr(struct ScriptContext *ctx)
 
 bool8 ScrCmd_compare_ptr_to_local(struct ScriptContext *ctx)
 {
-    const u8 value1 = *(const u8 *)ScriptReadWord(ctx);
+    const u8 value1 = *(const u8 *)ScriptReadPtr(ctx);
     const u8 value2 = ctx->data[ScriptReadByte(ctx)];
 
     ctx->comparisonResult = Compare(value1, value2);
@@ -429,7 +429,7 @@ bool8 ScrCmd_compare_ptr_to_local(struct ScriptContext *ctx)
 
 bool8 ScrCmd_compare_ptr_to_value(struct ScriptContext *ctx)
 {
-    const u8 value1 = *(const u8 *)ScriptReadWord(ctx);
+    const u8 value1 = *(const u8 *)ScriptReadPtr(ctx);
     const u8 value2 = ScriptReadByte(ctx);
 
     ctx->comparisonResult = Compare(value1, value2);
@@ -438,8 +438,8 @@ bool8 ScrCmd_compare_ptr_to_value(struct ScriptContext *ctx)
 
 bool8 ScrCmd_compare_ptr_to_ptr(struct ScriptContext *ctx)
 {
-    const u8 value1 = *(const u8 *)ScriptReadWord(ctx);
-    const u8 value2 = *(const u8 *)ScriptReadWord(ctx);
+    const u8 value1 = *(const u8 *)ScriptReadPtr(ctx);
+    const u8 value2 = *(const u8 *)ScriptReadPtr(ctx);
 
     ctx->comparisonResult = Compare(value1, value2);
     return FALSE;
@@ -996,7 +996,7 @@ bool8 ScrCmd_fadeinbgm(struct ScriptContext *ctx)
 bool8 ScrCmd_applymovement(struct ScriptContext *ctx)
 {
     u16 localId = VarGet(ScriptReadHalfword(ctx));
-    const void *movementScript = (const void *)ScriptReadWord(ctx);
+    const void *movementScript = (const void *)ScriptReadPtr(ctx);
 
     ScriptMovement_StartObjectMovementScript(localId, gSaveBlock1Ptr->location.mapNum, gSaveBlock1Ptr->location.mapGroup, movementScript);
     sMovingNpcId = localId;
@@ -1006,7 +1006,7 @@ bool8 ScrCmd_applymovement(struct ScriptContext *ctx)
 bool8 ScrCmd_applymovementat(struct ScriptContext *ctx)
 {
     u16 localId = VarGet(ScriptReadHalfword(ctx));
-    const void *movementScript = (const void *)ScriptReadWord(ctx);
+    const void *movementScript = (const void *)ScriptReadPtr(ctx);
     u8 mapGroup = ScriptReadByte(ctx);
     u8 mapNum = ScriptReadByte(ctx);
 
@@ -1268,7 +1268,7 @@ bool8 ScrCmd_release(struct ScriptContext *ctx)
 
 bool8 ScrCmd_message(struct ScriptContext *ctx)
 {
-    const u8 *msg = (const u8 *)ScriptReadWord(ctx);
+    const u8 *msg = (const u8 *)ScriptReadPtr(ctx);
 
     if (msg == NULL)
         msg = (const u8 *)ctx->data[0];
@@ -1278,7 +1278,7 @@ bool8 ScrCmd_message(struct ScriptContext *ctx)
 
 bool8 ScrCmd_pokenavcall(struct ScriptContext *ctx)
 {
-    const u8 *msg = (const u8 *)ScriptReadWord(ctx);
+    const u8 *msg = (const u8 *)ScriptReadPtr(ctx);
 
     if (msg == NULL)
         msg = (const u8 *)ctx->data[0];
@@ -1288,7 +1288,7 @@ bool8 ScrCmd_pokenavcall(struct ScriptContext *ctx)
 
 bool8 ScrCmd_messageautoscroll(struct ScriptContext *ctx)
 {
-    const u8 *msg = (const u8 *)ScriptReadWord(ctx);
+    const u8 *msg = (const u8 *)ScriptReadPtr(ctx);
 
     if (msg == NULL)
         msg = (const u8 *)ctx->data[0];
@@ -1301,7 +1301,7 @@ bool8 ScrCmd_messageautoscroll(struct ScriptContext *ctx)
 // Prints all at once. Skips waiting for player input. Only used by link contests
 bool8 ScrCmd_messageinstant(struct ScriptContext *ctx)
 {
-    const u8 *msg = (const u8 *)ScriptReadWord(ctx);
+    const u8 *msg = (const u8 *)ScriptReadPtr(ctx);
 
     if (msg == NULL)
         msg = (const u8 *)ctx->data[0];
@@ -1484,7 +1484,7 @@ bool8 ScrCmd_showcontestpainting(struct ScriptContext *ctx)
 
 bool8 ScrCmd_braillemessage(struct ScriptContext *ctx)
 {
-    u8 *ptr = (u8 *)ScriptReadWord(ctx);
+    u8 *ptr = (u8 *)ScriptReadPtr(ctx);
     struct WindowTemplate winTemplate;
     s32 i;
     u8 width, height;
@@ -1544,7 +1544,7 @@ bool8 ScrCmd_closebraillemessage(struct ScriptContext *ctx)
 
 bool8 ScrCmd_vmessage(struct ScriptContext *ctx)
 {
-    u32 msg = ScriptReadWord(ctx);
+    uintptr_t msg = ScriptReadPtr(ctx);
 
     ShowFieldMessage((u8 *)(msg - sAddressOffset));
     return FALSE;
@@ -1648,7 +1648,7 @@ bool8 ScrCmd_buffercontestname(struct ScriptContext *ctx)
 bool8 ScrCmd_bufferstring(struct ScriptContext *ctx)
 {
     u8 stringVarIndex = ScriptReadByte(ctx);
-    const u8 *text = (u8 *)ScriptReadWord(ctx);
+    const u8 *text = (u8 *)ScriptReadPtr(ctx);
 
     StringCopy(sScriptStringVars[stringVarIndex], text);
     return FALSE;
@@ -1656,7 +1656,7 @@ bool8 ScrCmd_bufferstring(struct ScriptContext *ctx)
 
 bool8 ScrCmd_vbuffermessage(struct ScriptContext *ctx)
 {
-    const u8 *ptr = (u8 *)(ScriptReadWord(ctx) - sAddressOffset);
+    const u8 *ptr = (u8 *)(ScriptReadPtr(ctx) - sAddressOffset);
 
     StringExpandPlaceholders(gStringVar4, ptr);
     return FALSE;
@@ -1665,7 +1665,7 @@ bool8 ScrCmd_vbuffermessage(struct ScriptContext *ctx)
 bool8 ScrCmd_vbufferstring(struct ScriptContext *ctx)
 {
     u8 stringVarIndex = ScriptReadByte(ctx);
-    u32 addr = ScriptReadWord(ctx);
+    uintptr_t addr = ScriptReadPtr(ctx);
 
     const u8 *src = (u8 *)(addr - sAddressOffset);
     u8 *dest = sScriptStringVars[stringVarIndex];
@@ -1889,7 +1889,7 @@ bool8 ScrCmd_dowildbattle(struct ScriptContext *ctx)
 
 bool8 ScrCmd_pokemart(struct ScriptContext *ctx)
 {
-    const void *ptr = (void *)ScriptReadWord(ctx);
+    const void *ptr = (void *)ScriptReadPtr(ctx);
 
     CreatePokemartMenu(ptr);
     ScriptContext_Stop();
@@ -1898,7 +1898,7 @@ bool8 ScrCmd_pokemart(struct ScriptContext *ctx)
 
 bool8 ScrCmd_pokemartdecoration(struct ScriptContext *ctx)
 {
-    const void *ptr = (void *)ScriptReadWord(ctx);
+    const void *ptr = (void *)ScriptReadPtr(ctx);
 
     CreateDecorationShop1Menu(ptr);
     ScriptContext_Stop();
@@ -1908,7 +1908,7 @@ bool8 ScrCmd_pokemartdecoration(struct ScriptContext *ctx)
 // Changes clerk dialogue slightly from above. See MART_TYPE_DECOR2
 bool8 ScrCmd_pokemartdecoration2(struct ScriptContext *ctx)
 {
-    const void *ptr = (void *)ScriptReadWord(ctx);
+    const void *ptr = (void *)ScriptReadPtr(ctx);
 
     CreateDecorationShop2Menu(ptr);
     ScriptContext_Stop();
diff --git a/src/script.c b/src/script.c
index 3136340d7..c57703341 100644
--- a/src/script.c
+++ b/src/script.c
@@ -174,6 +174,21 @@ u16 ScriptReadHalfword(struct ScriptContext *ctx)
     return value;
 }
 
+// Reads a pointer-width value out of the script stream. Identical to
+// ScriptReadWord on a 32-bit target; on a 64-bit one the assembler emits an
+// 8-byte pointer (ptr_stream in asm/macros/ptr.inc), so the interpreter has to
+// consume 8 bytes or it desynchronises from the stream.
+uintptr_t ScriptReadPtr(struct ScriptContext *ctx)
+{
+    uintptr_t value = 0;
+    u32 i;
+
+    for (i = 0; i < sizeof(uintptr_t); i++)
+        value |= (uintptr_t)(*(ctx->scriptPtr++)) << (i * 8);
+
+    return value;
+}
+
 u32 ScriptReadWord(struct ScriptContext *ctx)
 {
     u32 value0 = *(ctx->scriptPtr++);
diff --git a/tools/mapjson/mapjson.cpp b/tools/mapjson/mapjson.cpp
index 8d8ec4e4e..764b71793 100644
--- a/tools/mapjson/mapjson.cpp
+++ b/tools/mapjson/mapjson.cpp
@@ -139,24 +139,24 @@ string generate_map_header_text(Json map_data, Json layouts_data) {
     string mapName = json_to_string(map_data, "name");
     text << get_generated_warning("data/maps/" + mapName + "/map.json", true);
 
-    text << mapName << ":\n"
-         << "\t.4byte " << json_to_string(layout, "name") << "\n";
+    text << "\tpalign\n" << mapName << ":\n"
+         << "\tptr " << json_to_string(layout, "name") << "\n";
 
     if (map_data.object_items().find("shared_events_map") != map_data.object_items().end())
-        text << "\t.4byte " << json_to_string(map_data, "shared_events_map") << "_MapEvents\n";
+        text << "\tptr " << json_to_string(map_data, "shared_events_map") << "_MapEvents\n";
     else
-        text << "\t.4byte " << mapName << "_MapEvents\n";
+        text << "\tptr " << mapName << "_MapEvents\n";
 
     if (map_data.object_items().find("shared_scripts_map") != map_data.object_items().end())
-        text << "\t.4byte " << json_to_string(map_data, "shared_scripts_map") << "_MapScripts\n";
+        text << "\tptr " << json_to_string(map_data, "shared_scripts_map") << "_MapScripts\n";
     else
-        text << "\t.4byte " << mapName << "_MapScripts\n";
+        text << "\tptr " << mapName << "_MapScripts\n";
 
     if (map_data.object_items().find("connections") != map_data.object_items().end()
      && map_data["connections"].array_items().size() > 0 && json_to_string(map_data, "connections_no_include", true) != "TRUE")
-        text << "\t.4byte " << mapName << "_MapConnections\n";
+        text << "\tptr " << mapName << "_MapConnections\n";
     else
-        text << "\t.4byte NULL\n";
+        text << "\tptr NULL\n";
 
     text << "\t.2byte " << json_to_string(map_data, "music") << "\n"
          << "\t.2byte " << json_to_string(layout, "id") << "\n"
@@ -204,7 +204,7 @@ string generate_map_connections_text(Json map_data) {
 
     text << "\n" << mapName << "_MapConnections:\n"
          << "\t.4byte " << map_data["connections"].array_items().size() << "\n"
-         << "\t.4byte " << mapName << "_MapConnectionsList\n\n";
+         << "\tptr " << mapName << "_MapConnectionsList\n\n";
 
     return text.str();
 }
@@ -217,7 +217,7 @@ string generate_map_events_text(Json map_data) {
 
     ostringstream text;
     text << get_generated_warning("data/maps/" + mapName + "/map.json", true);
-    text << "\t.align 2\n\n";
+    text << "\tpalign\n\n";
 
     string objects_label, warps_label, coords_label, bgs_label;
 
@@ -345,7 +345,7 @@ string generate_map_events_text(Json map_data) {
         bgs_label = "NULL";
     }
 
-    text << mapName << "_MapEvents::\n"
+    text << "\tpalign\n" << mapName << "_MapEvents::\n"
          << "\tmap_events " << objects_label << ", " << warps_label << ", "
          << coords_label << ", " << bgs_label << "\n\n";
 
@@ -443,13 +443,13 @@ string generate_groups_text(Json groups_data) {
         text << group << "::\n";
         auto maps = groups_data[group].array_items();
         for (Json &map_name : maps)
-            text << "\t.4byte " << json_to_string(map_name) << "\n";
+            text << "\tptr " << json_to_string(map_name) << "\n";
         text << "\n";
     }
 
-    text << "\t.align 2\n" << "gMapGroups::\n";
+    text << "\tpalign\n" << "gMapGroups::\n";
     for (auto &group : groups_data["group_order"].array_items())
-        text << "\t.4byte " << json_to_string(group) << "\n";
+        text << "\tptr " << json_to_string(group) << "\n";
     text << "\n";
 
     return text.str();
@@ -600,14 +600,14 @@ string generate_layout_headers_text(Json layouts_data) {
              << "\t.incbin \"" << json_to_string(layout, "border_filepath") << "\"\n\n"
              << blockdata_label << "::\n"
              << "\t.incbin \"" << json_to_string(layout, "blockdata_filepath") << "\"\n\n"
-             << "\t.align 2\n"
+             << "\tpalign\n"
              << layoutName << "::\n"
              << "\t.4byte " << json_to_string(layout, "width") << "\n"
              << "\t.4byte " << json_to_string(layout, "height") << "\n"
-             << "\t.4byte " << border_label << "\n"
-             << "\t.4byte " << blockdata_label << "\n"
-             << "\t.4byte " << json_to_string(layout, "primary_tileset") << "\n"
-             << "\t.4byte " << json_to_string(layout, "secondary_tileset") << "\n";
+             << "\tptr " << border_label << "\n"
+             << "\tptr " << blockdata_label << "\n"
+             << "\tptr " << json_to_string(layout, "primary_tileset") << "\n"
+             << "\tptr " << json_to_string(layout, "secondary_tileset") << "\n";
         if (version == "firered") {
             text << "\t.byte " << json_to_string(layout, "border_width") << "\n"
                  << "\t.byte " << json_to_string(layout, "border_height") << "\n"
@@ -624,13 +624,13 @@ string generate_layouts_table_text(Json layouts_data) {
 
     text << get_generated_warning("data/layouts/layouts.json", true);
 
-    text << "\t.align 2\n"
+    text << "\tpalign\n"
          << json_to_string(layouts_data, "layouts_table_label") << "::\n";
 
     for (auto &layout : layouts_data["layouts"].array_items()) {
         string layout_name = json_to_string(layout, "name", true);
         if (layout_name.empty()) layout_name = "NULL";
-        text << "\t.4byte " << layout_name << "\n";
+        text << "\tptr " << layout_name << "\n";
     }
 
     return text.str();
diff --git a/tools/mid2agb/agb.cpp b/tools/mid2agb/agb.cpp
index 780f746e1..906b1d506 100644
--- a/tools/mid2agb/agb.cpp
+++ b/tools/mid2agb/agb.cpp
@@ -45,7 +45,8 @@ static int s_memaccParam2;
 
 void PrintAgbHeader()
 {
-    std::fprintf(g_outputFile, "\t.include \"MPlayDef.s\"\n\n");
+    std::fprintf(g_outputFile, "\t.include \"MPlayDef.s\"\n");
+    std::fprintf(g_outputFile, "\t.include \"asm/macros/ptr.inc\"\n\n");
     std::fprintf(g_outputFile, "\t.equ\t%s_grp, voicegroup%s\n", g_asmLabel.c_str(), g_voiceGroup.c_str());
     std::fprintf(g_outputFile, "\t.equ\t%s_pri, %u\n", g_asmLabel.c_str(), g_priority);
 
@@ -529,19 +530,22 @@ void PrintAgbFooter()
     int trackCount = g_agbTrack - 1;
 
     //std::fprintf(g_outputFile, "\n@******************************************************@\n");
-    std::fprintf(g_outputFile, "\t.align\t2\n");
+    // struct SongHeader starts with four u8 and then a pointer, so on a
+    // 64-bit target the struct itself has to be 8-aligned for that pointer
+    // to land where the compiler puts it.
+    std::fprintf(g_outputFile, "\tpalign\n");
     std::fprintf(g_outputFile, "\n%s:\n", g_asmLabel.c_str());
     std::fprintf(g_outputFile, "\t.byte\t%u\n", trackCount);
     std::fprintf(g_outputFile, "\t.byte\t%u\n", 0);
     std::fprintf(g_outputFile, "\t.byte\t%s_pri\n", g_asmLabel.c_str());
     std::fprintf(g_outputFile, "\t.byte\t%s_rev\n", g_asmLabel.c_str());
     std::fprintf(g_outputFile, "\n");
-    std::fprintf(g_outputFile, "\t.4byte\t%s_grp\n", g_asmLabel.c_str());
+    std::fprintf(g_outputFile, "\tptr\t%s_grp\n", g_asmLabel.c_str());
     std::fprintf(g_outputFile, "\n");
 
     // track pointers
     for (int i = 1; i <= trackCount; i++)
-        std::fprintf(g_outputFile, "\t.4byte\t%s_%u\n", g_asmLabel.c_str(), i);
+        std::fprintf(g_outputFile, "\tptr\t%s_%u\n", g_asmLabel.c_str(), i);
 
     std::fprintf(g_outputFile, "\n\t.end\n");
 }
